import initDataStateType from './model';

const dataReducer = (
  state: initDataStateType,
  action: unknown,
) => {
  switch (action) {
    case action:
      return {
        ...state,
      };

    default:
      return {
        ...state,
      };
  }
};

export default dataReducer;
